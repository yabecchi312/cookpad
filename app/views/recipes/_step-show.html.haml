- flows.each.with_index(1) do |flow, number|
  .step
    .step__head
      %h3
        = number
    .instruction
      .instruction__photo
        .image
          = flow.image.to_s
      .instruction__text
        = flow.text
